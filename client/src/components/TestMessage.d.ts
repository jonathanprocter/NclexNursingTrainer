function TestMessage.d() {
  return null;
}

import { ObservableNotification } from '@/types';
export interface TestMessage {
    frame: number;
    notification: ObservableNotification<any>;
    isGhost?: boolean;
}
//# sourceMappingURL=TestMessage.d.ts.map

export default TestMessage.d;
